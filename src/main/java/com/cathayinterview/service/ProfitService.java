package com.cathayinterview.service;

public interface ProfitService {
    double compareStartAndEnd(String startTime, String endTime);
    double compareStartAndEndRate(String startTime, String endTime);
}
